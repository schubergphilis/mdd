Text with `a  code  span, with a comma` and a [link, to a page](docs/some/where.md) plus an
autolink <https://example.com/a,b,c> and raw <span class="x">html</span> that
must all survive a reflow, since none of them may be broken across lines or altered.

Inline math $a, b$ and a footnote[^1] and an escaped \, comma stay put too.

[^1]: The footnote body is prose, so it reflows like any other paragraph would.
