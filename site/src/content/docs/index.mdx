---
title: mdd
description: Bidirectional sync between Markdown-in-git and Confluence/SharePoint.
template: splash
# The site turns this on globally, but a landing page has no single body of
# content whose age a reader could act on, and the footer's build date already
# says when the site was produced.
lastUpdated: false
# The only hand-authored page on the site, so the only one whose source really
# does live here. Every other page gets an editUrl from scripts/sync-docs.py.
editUrl: https://github.com/schubergphilis/mdd/edit/main/site/src/content/docs/index.mdx
hero:
  # Short, because the hero gives most of its width to the infographic and the
  # full name would wrap over three lines in what is left. The infographic's
  # centre card and the site title in the header both spell it out.
  title: MDD
  tagline: Bidirectional sync between Markdown-in-git and Confluence/SharePoint.
  # Two variants of one drawing. An SVG embedded this way is its own document
  # and cannot see the page's `data-theme`, so the palette has to be baked in;
  # `mise run derive-dark-svg-assets` generates the dark one and these copies
  # from assets/mdd-infographic.svg.
  image:
    light: ../../assets/mdd-infographic.svg
    dark: ../../assets/mdd-infographic-dark.svg
    alt: MDD sits between the Confluence and SharePoint sites an organisation already uses and a git repository of Markdown that engineers and AI agents work in, pulling pages one way and pushing reviewed edits back the other.
  actions:
    - text: Start
      link: /mdd/guide/install/
      icon: right-arrow
      variant: primary
    # Internal link, so a right arrow. `external`'s up-and-out arrow means
    # "leaves this site", which the design record does not.
    - text: Design record
      link: /mdd/design-record/
      icon: right-arrow
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

`mdd` does near-lossless roundtrips between a git repository of Markdown and
remote document management systems, so engineers and AI agents can work with
Markdown while everyone else keeps using the tools they know.

<Card title="Beta software - use with care" icon="warning">
	`mdd` is written almost entirely by AI agents under human review. There is
	no independent security review and no stable 1.0 release yet. Push
	operations (like `mdd confluence sync-space`) can clobber production
	systems. See the [Safety](/mdd/guide/safety/) page before you sync anything
	that matters.
</Card>

## Start here

<CardGrid>
	<LinkCard title="Install" href="/mdd/guide/install/" description="Get mdd running locally." />
	<LinkCard
		title="Quickstart"
		href="/mdd/guide/quickstart/"
		description="Convert, create and search files, offline, no tenant needed."
	/>
	<LinkCard
		title="Safety"
		href="/mdd/guide/safety/"
		description="What can destroy data, and how to avoid it."
	/>
	<LinkCard
		title="Concepts"
		href="/mdd/guide/concepts/"
		description="The mirror, the document IR, and sync direction."
	/>
</CardGrid>
